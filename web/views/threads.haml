%h1 Threads
%table
  %tr
    %th PID
    %th ID
    %th Name
    %th Status
    %th Priority
    %th Run Time
  - @threads.each do |thread|
    %tr
      %td= thread.pid
      %td= thread.object_id
      %td.align-left
        %pre= thread.name
      %td= thread.status
      %td= thread.priority
      %td= (Time.now.to_f - thread.started_at).round(1)
